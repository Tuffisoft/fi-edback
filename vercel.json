{
  "buildCommand": "cd dev && npm run build",
  "outputDirectory": "dev/.next",
  "installCommand": "npm install && npm run build && cd dev && npm install --install-links",
  "framework": "nextjs"
}
